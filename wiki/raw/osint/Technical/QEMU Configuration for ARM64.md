### QEMU Configuration for ARM64

If you are using ARM64, the UEFI firmware and configuration differ slightly from the 32-bit ARM configuration. Here is an example of how to configure QEMU to boot an ARM64 ISO image using UEFI.