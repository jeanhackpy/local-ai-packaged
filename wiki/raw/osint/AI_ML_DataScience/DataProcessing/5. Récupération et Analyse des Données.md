### 5. **Récupération et Analyse des Données**
Exemple d'extraction de données et d'analyse :
```python
import pandas as pd

def get_real_estate_data():
    query = "SELECT * FROM real_estate_data"
    data = pd.read_sql(query, engine)
    return data

# Analyse de base
data = get_real_estate_data()
print(data.describe())
```